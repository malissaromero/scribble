<!--<h1> <%= link_to "Back to Home", posts_path %> </h1>

<h1>Comments <%= link_to "(Create)", new_post_comment_path %> </h1>


  <% @comments.each do |comment| %>
  <div>
    <p> <%= link_to "#{comment.title}", comment %> </p>
    <p> <%= comment.author %> </p>
    <p> <%= comment.content %> </p>
  </div>
  <% end %>
